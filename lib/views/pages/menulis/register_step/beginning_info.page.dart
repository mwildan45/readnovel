import 'package:flutter/material.dart';

class BeginningInfoPage extends StatelessWidget {
  const BeginningInfoPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center();
  }
}
